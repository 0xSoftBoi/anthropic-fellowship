# Core
# Reproducibility note: LLM determinism comes from temperature=0 in the analyzers
# (claude/agentic/patch) + a single MODEL constant. Slither also needs a matching
# solc (e.g. via solc-select) to produce findings; without it the hybrid run is
# degraded — the analyzers now warn loudly when a tool is unavailable.
anthropic>=0.39
# Provider-agnostic model access (Anthropic, DeepSeek, Kimi, Qwen, MiniMax, GLM,
# or any local OpenAI-compatible server). MIT-licensed translation layer — no
# router, no markup. See agents/llm.py and docs/MULTI_MODEL.md.
litellm>=1.50
web3>=6.0
eth-abi
eth-utils

# Smart contract analysis
slither-analyzer
mythril

# Data
pandas
numpy

# Simulation
py-evm

# Utilities
tqdm
python-dotenv
requests
aiohttp
