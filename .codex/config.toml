#:schema https://developers.openai.com/codex/config-schema.json

# ECC Tools generated Codex baseline — reviewed & corrected for this repo.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "live"

# --- MCP servers ---------------------------------------------------------
# SECURITY NOTE: each `npx` server is auto-installed from npm on first use, and
# any `url` server sends data to a remote third party. Enable only what you use.
# github + playwright cover this repo's real workflows (PRs/CI and the
# preinstalled Chromium used to render diagrams).
[mcp_servers.github]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github"]

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@latest", "--extension"]

[mcp_servers.sequential-thinking]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-sequential-thinking"]

# Optional — uncomment to enable (auto-installs from npm / makes remote calls):
# [mcp_servers.context7]
# command = "npx"
# args = ["-y", "@upstash/context7-mcp@latest"]
#
# [mcp_servers.memory]
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-memory"]
#
# [mcp_servers.exa]   # remote third-party endpoint — queries leave your machine
# url = "https://mcp.exa.ai/mcp"

[features]
multi_agent = true

[agents]
max_threads = 6
max_depth = 1

[agents.explorer]
description = "Read-only codebase explorer for gathering evidence before changes are proposed."
config_file = "agents/explorer.toml"

[agents.reviewer]
description = "PR reviewer focused on correctness, security, and missing tests."
config_file = "agents/reviewer.toml"

[agents.docs_researcher]
description = "Documentation specialist that verifies APIs, framework behavior, and release notes."
config_file = "agents/docs-researcher.toml"
